function responsiveBanner() {
  // const bannerHeader = document.querySelector('.banner--header');
  // const banner = document.querySelector('.banner');
  // const aspectRatio = window.innerWidth / window.innerHeight;

  // console.log(aspectRatio)
  // // if(aspectRatio < 1.8) {
  // //   banner.style.height = `calc(100vh - (${bannerHeader.offsetWidth}px - 150px))`;
  // // }

  // if (WURFL.is_mobile && aspectRatio < 0.80) {
  //   banner.style.height = `calc(100vh - (${bannerHeader.offsetWidth}px - 100px))`;
  // }

  // console.log(WURFL.is_mobile);
}

export { responsiveBanner }